# 反向 DDD 梳理方法论

> 本文件是 `/lcw ddd` 系列命令的完整指引——方法论 + 页面模板。执行 DDD 梳理前必须阅读本文件。不需要额外读取 SCHEMA.md。

## 核心理念

你做的是**反向 DDD**——从代码考古出发重建领域模型，不是从需求出发设计领域模型。

最大的陷阱是**被代码现状带跑**：把烟囱当领域，把实现细节当业务概念。对抗这个陷阱的方法是**双向校验**：

- **自下而上**：从 `__wiki__/` 中提取代码事实（模块、接口、数据结构、调用关系）
- **自上而下**：从业务能力出发推导领域应该是什么样的
- **差异分析**：两边对不上的地方才是真正需要关注的问题

wiki 中的内容遵循**三层视角模型**：

- **现状理解层**（`__wiki__/` 根目录）：代码中实际呈现的结构。由 ingest/diff/lint 维护。
- **目标设计层**（`__wiki__/ddd/`）：领域模型应该是什么样。由 ddd 命令维护。
- **差距层**（`glossary.md` 的"差距"字段 + `ddd/gaps.md`）：两者的不一致 = 治理清单。

特殊的**双视角文档**：`glossary.md` 同时包含现状用法（As-Is）和领域定义（To-Be）。统一语言不单独建文件，直接增强 glossary.md 的条目。

---

## 命令结构

```
/lcw ddd                          # 交互式全流程
/lcw ddd strategic                # 战略层：领域识别
/lcw ddd tactical <context-name>  # 战术层：指定上下文的模型落地
/lcw ddd evolution                # 演进层：重构推进规划
/lcw ddd audit                    # 漂移检测：代码现实 vs DDD 模型
```

无参数时的默认行为：
1. 检查 `__wiki__/ddd/` 是否已有产出
   - 有 → 读取 `ddd/status.md` 进度，从上次断点继续
   - 无 → 从先决判断开始
2. 战略层完成后，交互式询问：先做哪个上下文的战术层？
3. 战术层完成一个后提示：继续下一个还是进入演进规划？

**迭代节奏**：战略层粗做一遍 → 挑一个上下文做战术层 → 反过来修正战略层 → 再做下一个上下文。这个往返是正常的，不要等战略层"完美"再开始战术层。

**统一语言是贯穿活动**：不是某个步骤做完才开始收集术语。从步骤 1 业务能力盘点起就持续增量更新 `glossary.md`——每个分析步骤结束时都检查是否有新术语、新歧义、新定义需要记录。统一语言的质量直接决定后续所有分析的准确度。

---

## 第零步：先决判断

首次运行 `/lcw ddd` 时，交互式引导用户做两个决策，持久化到 `ddd/decisions.md`。已有决策文件时跳过此步（但用户可随时用 `/lcw ddd` 重新审视）。

### 1. 梳理范围与目标

- **范围**：哪些 repo 纳入本次梳理？从 `__wiki__/repos/` 中列出清单供用户选择
- **目标**：梳理要解决什么问题？
  - 理解现状（新人 onboarding / 团队对齐）
  - 规划重构（烟囱合并 / 架构升级）
  - 支撑新需求设计（在已有架构上长出新能力）
  - 持续治理（建立领域模型并保持与代码同步）
- **输出偏好**：词汇表语言（中/英/双语）、图表格式（ASCII art / Mermaid）

### 2. 系统架构定位（仅横切系统需要）

仅当系统属于横切类型（治理、安全、合规、监控等嵌入所有业务的系统）时，需要选择架构模式：集中式平台 / 分布式框架 / 两层分离。纯业务系统标"不适用"跳过。这个判断不先做，后面的限界上下文划分会反复推翻。

---

## 第一层：战略层

命令：`/lcw ddd strategic`

### 输入

从 `__wiki__/` 中读取：

| 页面类型 | 用途 |
|---------|------|
| `overview.md` | 系统全景、业务能力地图 |
| `domains/*.md` | 已识别的领域（代码事实层面） |
| `interfaces/*.md` | 跨系统接口、已有 DDD 关系类型标注 |
| `glossary.md` | 术语表 |
| `flows/*.md` | 端到端业务流程 |
| `modules/*.md` | 模块详情（按需深入） |
| `issues/*.md` | 已知问题（可能揭示领域边界问题） |

如果 wiki 尚未摄入或内容不足，提示用户先执行 `/lcw ingest`。

### 分析步骤

#### 1. 业务能力盘点

从 wiki 的 domains 和 modules 中提取系统对外暴露的所有业务能力：

- 读取每个 `domains/*.md` 的 `capabilities` 字段
- 读取每个 `modules/*.md` 的 Public API 章节
- 去重合并，按业务含义（而非代码结构）分组
- 标注每个能力：对外暴露 / 仅内部使用 / 未明确

产出：能力清单（写入 `ddd/panorama.md` 的"业务能力盘点"章节）

#### 2. 子域分类

将能力分组为三类：

| 类型 | 判据 | 投入策略 |
|------|------|---------|
| 核心域 | 直接产生业务差异化价值的能力 | 重点投入、精心建模 |
| 支撑域 | 业务必需但不直接产生差异化的能力 | 标准化、够用即可 |
| 通用域 | 通用基础设施能力（无业务特殊性） | 尽量复用外部方案 |

分类时警惕两个常见错误：
- 把所有东西都标为"核心"——如果一切都是核心，就没有核心
- 把重要性等同于核心域——"重要"不等于"差异化"，数据库很重要但通常是通用域

这一步需要人工确认——呈现分类方案，说明判据，让用户调整。

产出：更新 `ddd/panorama.md` 的"子域分类"章节

#### 3. 烟囱检测

在子域分类的基础上，找"实质上做同一件事"的多个实现：

- 在 wiki 中搜索相似的 API 签名、数据结构、业务逻辑描述
- 对比不同模块中名称相似或职责重叠的组件
- 重点关注：同一业务操作在不同入口（HTTP API / RPC / 定时任务 / 消息消费者）的多份实现
- 对每个烟囱记录：存在于哪些模块、差异点、统一的阻碍

烟囱检测放在子域分类之后，因为需要先有领域上下文才能区分"跨域的合理重复"和"同域的无意烟囱"。

产出：写入 `ddd/panorama.md` 的"烟囱报告"章节

> **Checkpoint 1：子域 + 烟囱确认**
>
> 到这里暂停，向用户呈现：
> 1. 业务能力清单（按子域分组）
> 2. 核心域 / 支撑域 / 通用域的分类方案及判据
> 3. 烟囱检测的发现
>
> 等用户确认或调整后再继续。子域分类是后续所有工作的基础——如果这里分错了，限界上下文、上下文映射、统一语言全部会歪。调整成本在这里最低，往后越来越高。

#### 4. 限界上下文划分

基于能力分组 + 数据边界 + 团队边界 + 烟囱分析，提出上下文划分方案：

- 一个限界上下文 = 一组内聚的业务能力 + 统一的领域语言 + 明确的边界
- 边界判据：
  - 数据边界：哪些实体只在这个上下文内被修改？
  - 语言边界：同一个词在不同地方含义不同？（从 glossary.md 的"多义"条目获取）
  - 团队边界：谁负责这部分代码？（从 activities/ 的作者数据获取）
  - 变更频率：一起变更的代码通常属于同一个上下文

对每个上下文写清楚：
- 职责边界（做什么、不做什么）
- 对外契约（暴露什么接口）
- 拥有的核心数据（哪些实体归这个上下文"拥有"）

#### 5. 上下文映射

从 `interfaces/*.md` 中提取现有的跨上下文关系，补充遗漏的：

| 关系类型 | 含义 | 老系统常见度 |
|---------|------|------------|
| Partnership | 两个上下文紧密合作，同步演进 | 低 |
| Customer-Supplier | 上游提供，下游消费，上游主导 | 中 |
| Conformist | 下游完全迁就上游模型 | 中 |
| Anti-Corruption Layer | 下游用转换层隔离上游的脏模型 | 高 |
| Shared Kernel | 两个上下文共享一小部分模型 | 中 |
| Open Host Service | 上游提供标准化协议供多下游使用 | 中 |
| Separate Ways | 各自独立，不集成 | 高 |

老系统改造里"Separate Ways"和"ACL"用得最多——不要回避承认这一点。

步骤 4 和 5 的产出合并写入 `ddd/contexts.md`（上下文定义 + 映射关系在同一文件中，因为它们天然一起阅读和维护）。

> **Checkpoint 2：限界上下文确认**
>
> 到这里再次暂停，向用户呈现：
> 1. 限界上下文清单（每个上下文的职责边界、对外契约、核心数据）
> 2. 上下文映射全景图（关系类型 + 集成方式）
> 3. 与 wiki `domains/` 现有划分的对比——哪些一致、哪些要拆分或合并
> 4. 边界判据说明（为什么这样划分而非那样划分）
>
> 等用户确认后再做统一语言整合和差异报告。上下文边界一旦确定，后续的统一语言、战术层建模都基于此。如果用户对边界有异议，现在改成本最低。

#### 6. 统一语言整合

> 注意：统一语言是贯穿整个战略层的持续活动（见上方"迭代节奏"）。这一步不是"开始做统一语言"，而是在前 5 步持续收集的基础上做一次系统性整合。

直接增强 `glossary.md`（双视角文档），不单独生成词汇表文件。

以 `glossary.md` 现有条目为基础：

- 提取所有 status 为"不一致"和"多义"的术语
- 对每个核心名词，在条目中补充 DDD 字段：
  - **领域定义（To-Be）**：该术语在领域模型中的精确定义。如果需要拆分为多个概念，列出每个概念的名称和定义
  - **差距**：现状用法与领域定义之间的不一致
  - **决策**：消解差距的行动方向
  - **消解状态**：未开始 / 进行中 / 已消解
- 标注哪些不一致是"历史遗留但无害"，哪些是"必须修正否则会持续引发误解"

写入时**保留** glossary.md 中由 ingest/diff/lint 维护的"现状用法"字段。

产出：更新 `glossary.md`（补充 DDD 相关字段）

#### 7. 双向校验与差异报告

将以上分析结果与 wiki 中的代码事实对比：

- wiki 中的 `domains/` 划分 vs DDD 分析的上下文划分——边界是否一致？
- wiki 中的 `interfaces/` 关系类型 vs DDD 分析的映射关系——是否完整？
- wiki 中的模块归属 vs DDD 分析的上下文归属——有没有模块被错分？
- 代码中实际存在的依赖 vs DDD 期望的依赖——有没有不该存在的耦合？

对每个差异标注：
- 差异描述
- 严重程度（提示 / 警告 / 阻塞）
- 建议行动

产出：`ddd/gaps.md`（仅记录战略层发现的初始差异）

### 战略层完成标准

战略层做完后，必须能回答这个问题：

> 从现在的烟囱状态，到底有几个限界上下文？每个上下文的对外契约是什么？

回答不出来，就是没做完。向用户呈现总结时，显式检验这个标准。

### 战略层产出汇总

| 文件 | 内容 |
|------|------|
| `ddd/status.md` | 状态总览（每次操作后更新） |
| `ddd/decisions.md` | 先决决策（架构定位、范围、目标） |
| `ddd/panorama.md` | 领域全景图（能力盘点 + 子域分类 + 烟囱报告） |
| `ddd/contexts.md` | 限界上下文（定义 + 映射关系） |
| `ddd/gaps.md` | 初始差异报告（战略层发现） |
| `glossary.md`（增强） | 统一语言（DDD 字段） |

---

## 第二层：战术层

命令：`/lcw ddd tactical <context-name>`

对指定的限界上下文做深度建模。`<context-name>` 必须是 `ddd/contexts.md` 中已定义的上下文名称。

不需要一次性做完所有上下文。先挑一两个核心上下文做透，验证方法可行，再铺开。

**按子域类型调整深度**：

| 子域类型 | 战术层深度 | 做什么 | 不做什么 |
|---------|----------|--------|---------|
| 核心域 | 全量（7 步） | 全部分析步骤 | — |
| 支撑域 | 精简 | 聚合根识别 + 领域事件 + 防腐层 | 贫血检测、领域服务详细分析 |
| 通用域 | 通常跳过 | 仅在耦合严重时做防腐层设计 | 完整战术建模 |

投入应该跟着业务价值走——对通用域做精细建模是浪费。

### 输入

- `ddd/contexts.md` — 该上下文的职责边界和映射关系
- `glossary.md` — 统一语言（读取"领域定义"字段）
- 该上下文涉及的 `modules/*.md` — 模块详情
- 该上下文涉及的 `domains/*.md` — 领域页面
- 该上下文涉及的 `flows/*.md` — 端到端业务流程（Event Storming 的 markdown 版本——每个 flow 已按 触发→命令→聚合→领域事件 结构组织，是提取领域事件和聚合根的主要来源）
- 源代码 — 按需读取验证（wiki 描述可能过时，关键判断要回源码确认）

### 分析步骤

#### 1. 数据模型提取

从 wiki 的模块页面提取现有数据结构：

- `modules/*.md` 的 Public API 和 Internal logic 章节
- `domains/*.md` 的"核心实体与聚合"章节
- 关注 frontmatter 中的 `model_style` 字段

#### 2. 聚合根识别

聚合根 = 一致性边界的守护者。识别判据：

- 哪些实体的修改必须是原子的？（事务边界）
- 哪个实体"拥有"其他实体的生命周期？（创建和销毁权）
- 外部引用只能通过谁的 ID 来访问？（引用入口）

不要照搬现有的数据库表结构。一张"上帝表"可能对应多个聚合根，多张关联表可能只是一个聚合的不同视角。

对每个聚合根记录：
- 聚合根实体
- 聚合内实体（被聚合根管理生命周期的实体）
- 值对象（无独立身份的属性组合）
- 不变量约束（invariants，该聚合必须始终满足的业务规则）

#### 3. 贫血检测

如果 `model_style` 是 `anemic` 或 `procedural`：

- 找出所有对该实体数据进行操作的 Service / Manager / Handler / Processor
- 判断这些操作中哪些是实体的内聚职责（应该搬进实体）
- 哪些是跨聚合的协调逻辑（应该放在领域服务或通过领域事件解耦）
- 哪些纯粹是基础设施关注点（应该放在应用层或基础设施层）

**贫血不一定是问题**。在 Go/Rust 等语言中 procedural 风格可能是刻意的技术选择。真正的问题信号是：同一组数据的操作散布在 5+ 个 Service 中且互相不知道对方的存在——这意味着业务规则没有内聚的守护者，修改一个规则需要同时改多处。如果逻辑集中在少数几个 Service 中且职责清晰，procedural 风格本身不构成问题。

标注每个操作的建议归属，但不强制要求立即重构——这是现状分析，不是重构计划。

#### 4. 领域事件提取

从 `flows/*.md` 和 `modules/*.md` 中提取（`flows/*.md` 已按 Event Storming 结构组织，是最高效的事件来源）：

- 状态变更（entity.Status 从 A 变为 B 时，谁需要知道？）
- 异步通知（消息队列、事件总线中的消息）
- 跨上下文的数据同步（一个上下文的变更触发另一个上下文的动作）

对每个领域事件记录：
- 事件名称（过去时态，如 `PolicyEvaluated`、`RuleSetUpdated`）
- 触发条件
- 生产者（哪个聚合根产出）
- 消费者（谁订阅）
- 载荷（携带哪些数据）
- 是否需要持久化（事件溯源 vs 即发即弃）

#### 5. 防腐层设计

对该上下文与外部系统的每个交互点：

- 现有接口是"干净"的还是"脏"的？（脏 = 暴露了对方的内部实现细节）
- 是否需要 ACL 隔离？如果需要，转换层应该做什么？
- ACL 的方向：是保护本上下文不被外部污染，还是保护外部不被本上下文的变更影响？

防腐层是老系统重构的核心手段——用 ACL 把"旧的脏接口"和"新的干净模型"隔开，新模型可以在不动老代码的前提下先长出来。

#### 6. 领域服务识别

跨聚合的逻辑放在领域服务里，但要警惕"杂物间"倾向：

- 领域服务应该只包含**需要协调多个聚合的业务逻辑**
- 如果一个领域服务只操作一个聚合 → 这个逻辑应该在聚合根里
- 如果一个领域服务变成了几十个方法的大类 → 需要拆分或重新审视聚合边界

#### 7. 跨聚合协调模式

当一个业务流程跨越多个聚合根时，光有领域事件不够——需要明确的协调模式。遗留系统中常见的情况是一个大事务覆盖多个聚合，重构时必须选择协调策略：

| 模式 | 适用场景 | 一致性 | 复杂度 |
|------|---------|--------|--------|
| Saga（编排式） | 各聚合通过事件链自行推进 | 最终一致 | 低（但调试难） |
| Process Manager（协调式） | 中心协调器控制流程步骤 | 最终一致 | 中 |
| 大事务（现状） | 所有操作在同一事务中 | 强一致 | 低（但扩展差） |

对每个跨聚合的业务流程：
- 当前是用什么模式协调的？（通常是大事务或隐式的调用链）
- 目标态建议用哪种模式？
- 补偿逻辑（回滚/重试）现在有没有？缺了哪些？

不要默认推荐 Saga——如果当前的大事务在性能和规模上没有问题，记录现状即可，在演进层再规划迁移。

### 产出物

每个上下文一个文件：`ddd/tactical/{context-name}.md`，用章节组织所有战术层内容。

| 章节 | 内容 |
|------|------|
| 聚合根清单 | 聚合根 + 聚合内实体 + 值对象 + 不变量 + 贫血检测结果 |
| 领域事件 | 事件目录（名称、触发条件、生产者、消费者、载荷、持久化需求） |
| 防腐层 | 每个外部交互点的 ACL 设计（方向、转换规则、当前状态） |
| 领域服务 | 服务清单（职责、协调的聚合、杂物间风险标注） |
| 跨聚合协调 | 跨聚合业务流程的协调模式（Saga/Process Manager/大事务）及补偿逻辑 |

单文件的好处：一个上下文的所有战术模型在一处阅读和维护，不用在多个文件间跳转。当某个章节特别长（如聚合根超过 5 个、事件超过 20 个）时，可以拆分为独立文件并在主文件中用链接引用。

### 战术层完成后

完成一个上下文的战术层后：
1. 回看 `ddd/contexts.md`——这个上下文的边界定义是否需要调整？如果是，更新战略层产出
2. 回看 `glossary.md`——是否发现了新的术语不一致？如果是，补充 DDD 字段（领域定义、差距、决策）
3. 更新 `ddd/status.md` 进度表
4. 提示用户：继续下一个上下文，还是进入演进规划？

---

## 第三层：演进层

命令：`/lcw ddd evolution`

将战略层和战术层的产出落到工程节奏上。前置条件：战略层已完成，且至少一个核心上下文的战术层已完成。

### 输入

- 所有 `ddd/` 目录下的产出
- `__wiki__/issues/*.md` — 已知问题
- `__wiki__/activities/*.md` — 变更频率数据（如果有）

### 分析步骤

#### 1. 现状成熟度评估

对每个限界上下文评估：

| 维度 | 来源 | 评分标准 |
|------|------|---------|
| 烟囱程度 | `ddd/panorama.md` 烟囱报告章节 | 有多少重复实现？ |
| 耦合度 | `ddd/contexts.md` 映射关系章节 + `interfaces/*.md` | 跨上下文依赖有多紧？ |
| 模型健康度 | `ddd/tactical/*.md` | 贫血程度、聚合边界清晰度 |
| 变更频率 | `activities/*.md` | 过去 N 个月改动频率 |
| 问题密度 | `issues/*.md` | 该区域的问题数量和严重度 |

#### 2. 优先级排序

重构优先级 = 技术紧迫度 × 组织就绪度。

**技术紧迫度** = 耦合度高 × 变更频率高 × 问题密度高 的交集。不是按架构师偏好排序，而是按"不改的成本最高"排序。

**组织就绪度**——技术上最该改的不一定现在能改：
- 团队是否有足够的人力和能力执行这个重构？
- 是否处于变更冻结期或业务高峰期？
- 有没有业务侧的优先级约束（如正在进行的大需求会冲突）？
- 相关团队是否对重构方向有共识？

技术紧迫度决定"应该先改什么"，组织就绪度决定"能先改什么"。两者冲突时，向用户呈现冲突而非替他们做决定。明确标注排序依据，让用户可以挑战和调整。

#### 3. 迁移路径规划

对每个需要重构的上下文：

- **目标态**：DDD 模型中这个上下文应该是什么样？
- **过渡态**：中间态什么样？承认"过渡架构会存在很长时间"比假装"半年就能切完"更务实
- **步骤**：
  1. 先建 ACL 隔离新旧
  2. 在 ACL 后面长出新模型
  3. 逐步把流量从旧路径切到新路径
  4. 旧代码退役
- **灰度方案**：怎么逐步切流量？
- **回滚预案**：出问题怎么回退？

#### 4. 质量基线

定义重构期间不能退化的指标：

- 功能正确性（核心业务流程是否正常）
- 性能指标（延迟、吞吐）
- 可观测性覆盖（日志、监控、告警是否完备）
- 特定于业务领域的指标（例如治理类系统的：卡控有效性、决策延迟、误判率）

没有基线，重构后只能口头说"应该没退化"。

### 产出物

| 文件 | 内容 |
|------|------|
| `ddd/evolution/roadmap.md` | 重构路线图：优先级排序 + 依据 + 预估节奏 |
| `ddd/evolution/transition.md` | 过渡架构描述：每个中间态的架构图 |
| `ddd/evolution/migration-{context}.md` | 逐上下文的迁移指南、灰度方案、回滚预案 |
| `ddd/evolution/baseline.md` | 质量基线定义与回归指标 |

---

## 漂移检测

命令：`/lcw ddd audit`

核心目的：把 DDD 从一次性梳理变成持续治理。

### 执行逻辑

1. 读取 `__wiki__/ddd/` 下所有产出（DDD 模型 = "应然态"）
2. 读取 `__wiki__/` 根目录的最新代码事实页面（代码现实 = "实然态"）
3. 对比分析：

| 检查项 | 对比方式 | 发现问题时的处理 |
|--------|---------|----------------|
| 上下文边界 | `ddd/contexts.md` vs `domains/*.md` | 有新模块不属于任何已定义的上下文？ |
| 映射关系 | `ddd/contexts.md` 映射章节 vs `interfaces/*.md` | 有新接口未在映射中？关系类型变了？ |
| 统一语言 | `glossary.md` 中的"领域定义"vs"现状用法" | 有新的不一致术语出现？已消解的差距是否又回退？ |
| 聚合边界 | `ddd/tactical/*.md` 的"聚合根清单"章节 vs `modules/*.md` | 聚合内新增了不该有的依赖？ |
| 事件目录 | `ddd/tactical/*.md` 的"领域事件"章节 vs `flows/*.md` | 有新的跨上下文通信未通过事件？ |

4. 产出差异报告，对每个差异标注：
   - **代码追上了模型**：代码变更使得实际结构更接近 DDD 模型 → 正面信号
   - **代码偏离了模型**：代码变更背离了 DDD 模型 → 需要关注
   - **模型需要更新**：业务本身变了，DDD 模型已过时 → 更新模型

5. 产出写入 `ddd/audit-{YYYY-MM-DD}.md`（每次审计独立文件，不覆盖历史）

### 推荐用法

每次执行 `/lcw ingest` 或 `/lcw diff` 后，跑一次 `/lcw ddd audit`。可以在 `/lcw diff` 或 `/lcw lint` 完成后自动提示用户。

### audit 与 gaps 的关系

- `ddd/gaps.md`：战略层步骤 7 产出的**初始差异报告**，记录分析时发现的 As-Is 与 To-Be 之间的结构性差异。相对稳定，随战略层修订更新。
- `ddd/audit-*.md`：每次审计产出的**增量漂移报告**，记录代码变更后与 DDD 模型的偏差。随时间积累。

两者职责不同：gaps 是"一开始就知道的差距"，audit 是"后来漂出来的偏差"。

---

## 状态总览

每次 DDD 操作（strategic / tactical / evolution / audit）完成后，更新 `ddd/status.md`。这是 DDD 分析的仪表盘——新读者打开这一个文件就能了解整体进度，不用逐个翻阅产出文件。

页面内容：

- **梳理范围**：纳入的 repo 清单（链接到 `ddd/decisions.md`）
- **进度表**：

```markdown
| 层次 | 产出 | 状态 | 最后更新 |
|------|------|------|---------|
| 先决判断 | [[ddd/decisions]] | ✓ 已完成 | 2026-05-01 |
| 战略-全景图 | [[ddd/panorama]] | ✓ 已确认 | 2026-05-02 |
| 战略-上下文 | [[ddd/contexts]] | ✓ 已确认 | 2026-05-03 |
| 战略-差异 | [[ddd/gaps]] | ✓ 已完成 | 2026-05-03 |
| 战术-OrderContext | [[ddd/tactical/order]] | 进行中 | 2026-05-08 |
| 战术-PaymentContext | — | 未开始 | — |
| 演进-路线图 | — | 未开始 | — |
| 最近审计 | [[ddd/audit-2026-05-10]] | 2026-05-10 | — |
```

- **关键发现摘要**：3-5 条最重要的发现（链接到详细页面）
- **下一步行动**：当前应该做什么

---

## 与 wiki 的集成约定

### 读取（DDD → wiki）

| wiki 页面 | DDD 怎么用 |
|-----------|-----------|
| `domains/*.md` | 子域划分的原料，但 DDD 可能重新划分边界 |
| `interfaces/*.md` | 复用 relationship 标注，补充遗漏的映射关系 |
| `glossary.md` | 统一语言的起点 |
| `modules/*.md` 的 `model_style` | 贫血检测的直接输入 |
| `flows/*.md` | 领域事件提取的主要来源（Event Storming 结构） |
| `activities/*.md` | 变更频率、团队边界的数据源 |
| `issues/*.md` | 问题密度评估 |

### 回写（DDD → wiki）

DDD 分析过程中发现的内容回写到 wiki：

- 新发现的问题 → 创建 `issues/*.md`
- 术语的领域定义和差距 → 更新 `glossary.md` 条目的 DDD 字段（领域定义、差距、决策、消解状态）
- 新发现的概念 → 创建 `concepts/*.md`
- 领域的 DDD 定位 → 更新 `domains/*.md` 的 `ddd_context`/`ddd_status` frontmatter 字段 + 底部"DDD 视角"章节

DDD 不修改 `modules/*.md`、`repos/*.md` 的主体内容——这些是代码事实，由 ingest/diff/lint 维护。对于 `domains/*.md`，DDD 只写入 frontmatter 的 DDD 字段和底部的"DDD 视角"章节，不修改主体内容。对于 `glossary.md`，DDD 只写入 DDD 字段，不修改"现状用法"。

**例外**：DDD 分析确认了上下文映射关系后，可以回写 `interfaces/*.md` 的 `relationship`、`upstream`、`downstream` 字段——这属于"代码事实的补充"（从代码中可观测但 ingest 未捕获的关系类型），而非 DDD 目标态。

### 日志

所有 DDD 操作在 `log.md` 中记录：

```
## [ISO时间] ddd strategic | 战略层分析
- 识别子域: N 个（核心: A, 支撑: B, 通用: C）
- 识别限界上下文: M 个
- 烟囱: K 组
- 产出: ddd/panorama.md, ddd/contexts.md, ddd/gaps.md
- 更新: glossary.md（补充 DDD 字段 N 条）, domains/*.md（补充 DDD 视角 M 个）

## [ISO时间] ddd tactical | {context-name}
- 聚合根: N 个
- 领域事件: M 个
- 防腐层: K 个
- 产出: ddd/tactical/{context-name}.md

## [ISO时间] ddd evolution | 演进规划
- 评估上下文: N 个
- 优先重构: M 个
- 产出: ddd/evolution/roadmap.md, ...

## [ISO时间] ddd audit | 漂移检测
- 检查项: N 个
- 差异: M 个（正面: A, 偏离: B, 模型过时: C）
- 产出: ddd/audit-{YYYY-MM-DD}.md
```

---

## DDD 页面模板

以下是所有 DDD 产出文件的 frontmatter 和章节约定。

### ddd/status.md

```yaml
---
type: ddd-status
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：梳理范围、进度表、关键发现摘要、下一步行动。

### ddd/decisions.md

```yaml
---
type: ddd-decisions
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：梳理范围（repo 清单）、梳理目标、输出偏好、系统架构定位（仅横切系统）。

### ddd/panorama.md

```yaml
---
type: ddd-panorama
scope: [纳入梳理的 repo 列表]
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：

- **业务能力盘点**：表格列出所有业务能力（能力名 / 描述 / 暴露方式 / 所属子域 / 实现模块）
- **子域分类**：核心域 / 支撑域 / 通用域三类，每类列出包含的能力，标注分类依据
- **子域全景图**：ASCII art 或 Mermaid 图，展示子域之间的关系
- **烟囱报告**：每组烟囱一个子章节（烟囱名称 / 存在位置 / 差异分析 / 合并建议）

### ddd/contexts.md

```yaml
---
type: ddd-contexts
context_count: {N}
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

**上下文定义**——每个上下文一个章节：

- **职责边界**：做什么、不做什么
- **对外契约**：暴露的接口（命令 + 查询）
- **核心数据**：该上下文"拥有"的实体
- **所属子域**：核心 / 支撑 / 通用
- **实现现状**：当前由哪些 repo/module 承载（链接到 wiki 页面）
- **边界判据**：为什么这样划分（数据边界 / 语言边界 / 团队边界 / 变更频率）

**映射关系**——合并在同一文件中：

- **映射全景图**：ASCII art 或 Mermaid 图
- **关系明细**：表格（上游 / 下游 / 关系类型 / 集成方式 / 数据一致性 / 备注）
- **关系类型统计**：各类型的数量分布

### ddd/gaps.md

```yaml
---
type: ddd-gaps
gap_count: {N}
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

战略层初始差异报告。每个差异一行或一节：差异描述、类型（边界不一致 / 关系缺失 / 模块错分 / 非法耦合）、严重程度（提示 / 警告 / 阻塞）、相关页面、建议行动。

### ddd/audit-{YYYY-MM-DD}.md

```yaml
---
type: ddd-audit
audit_date: {YYYY-MM-DD}
diff_count: {N}
---
```

增量漂移报告。每个差异标注类型：代码追上了模型（正面）/ 代码偏离了模型（关注）/ 模型需要更新（过时）。

### ddd/tactical/{context-name}.md

```yaml
---
type: ddd-tactical
context: {context-name}
aggregate_count: {N}
event_count: {N}
acl_count: {N}
service_count: {N}
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：

- **上下文概述**：一段话描述该上下文的核心职责（链接到 `[[ddd/contexts]]`）
- **聚合根清单**：每个聚合根一个子章节（聚合根实体、聚合内实体、值对象、不变量约束、代码现状）
- **贫血检测结果**：被错放在 Service/Manager 中的实体行为清单，标注建议归属
- **领域事件**：表格（事件名称 / 触发条件 / 生产者聚合 / 消费者 / 载荷摘要 / 持久化需求 / 当前实现方式）
- **防腐层**：每个 ACL 一个子章节（隔离对象、方向、转换规则、当前状态）
- **领域服务**：每个服务一行或一节（服务名称、职责、协调的聚合、杂物间风险、当前实现）
- **跨聚合协调**（核心域必含，支撑域按需）：每个跨聚合流程一节（业务流程名称、涉及聚合根、当前协调模式、目标协调模式、补偿逻辑现状）

### ddd/evolution/roadmap.md

```yaml
---
type: ddd-roadmap
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：现状评估（表格含组织就绪度列）、优先级排序及依据、路线图（阶段/目标/上下文/工作量/前置依赖）、风险与假设。

### ddd/evolution/transition.md

```yaml
---
type: ddd-transition
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

每个阶段一个章节：架构图、新旧共存、切流策略。

### ddd/evolution/migration-{context}.md

```yaml
---
type: ddd-migration
context: {context-name}
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：目标态、迁移步骤、灰度方案、回滚预案、验证方法。

### ddd/evolution/baseline.md

```yaml
---
type: ddd-baseline
created: {YYYY-MM-DD}
last_updated: {YYYY-MM-DD}
---
```

必含章节：通用指标、业务指标、测量方法、红线。

---

## 常见陷阱

在执行过程中时刻警惕：

1. **把烟囱当领域**：三个团队各自做了一套"风控"，不代表有三个风控域——很可能只有一个核心风控域和两个应该被合并的烟囱
2. **把贫血模型当领域模型**：wiki 抽出来的数据结构如果直接拿来当领域模型，大概率是贫血的。必须把"行为"补回模型里
3. **回避难听的结论**：大量的"Separate Ways"和"ACL"在老系统里是常态，不要美化为"Partnership"
4. **子域分类一刀切**：团队容易把自己负责的所有东西都标为"核心域"。核心域的判据是"差异化价值"，不是"重要性"
5. **忽略团队边界**：康威定律是真实的。上下文划分必须考虑团队结构，否则会产生"组织上无法维护"的领域模型
6. **过早追求完美**：战略层的上下文划分永远不会在第一遍就完美。先做粗分，在战术层验证后再修正
7. **产出文档化但不被使用**：DDD 的最终产出不是文档，是团队共识。通过 audit 机制保持 DDD 模型与代码的持续对齐，让文档保持活性
8. **统一语言放到最后才做**：术语不一致往往是领域边界问题的最早信号。如果把统一语言当成最后一步，前面的子域分类和上下文划分可能已经建立在模糊的术语基础上了
